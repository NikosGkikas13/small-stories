export type { StoryFormData } from "@/lib/validators";

export type StoryLength = "short" | "medium";

export type StoryFormat = "story" | "poem";

export type GenerationStatus = "idle" | "generating" | "done" | "error";
